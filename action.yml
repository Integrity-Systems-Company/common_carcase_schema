name: 'Create MD Files'
description: 'Read in the JSON schema files and generate MD documentation files for the schema'
outputs:
  files:
    description: 'The list of found JSON files'
  content:
    description: 'The content of the last file read'
runs:
  using: 'node12'
  main: 'index.js'
